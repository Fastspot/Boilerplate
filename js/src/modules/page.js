/*-------------------------------------------
  Page
-------------------------------------------*/

	Site.modules.Page = (function($, Site) {

		function init() {
			//
		}

		// Hook into main init routine
		Site.onInit.push(init);

		return {
			//
		};
	})(jQuery, Site);
