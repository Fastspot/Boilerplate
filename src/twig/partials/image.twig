{#
	{% include par ~ "image.twig" with {
		class: "media",
		image: item.image,
		dimension: img.wide.med,
		itemprop: ""
	} %}
#}

<img class="{{class}}_image" src="//images.fastspot.com/{{vars.images}}/{{dimension}}/{{image}}" alt=""{% if itemprop %} itemprop="{{itemprop}}"{% endif %}>
