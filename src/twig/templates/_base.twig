{# Shortcuts For Use Within Components #}
{% set feat = "../components/feature/" %}
{% set full = "../components/full-width/" %}
{% set inc = "../components/in-content/" %}
{% set side = "../components/sidebar/" %}
{% set par = "../../partials/" %}

{% set grid = true %}

{% import '../partials/macros.twig' as site %}

{% include '../partials/base/header.twig' with {
	layout: page.layout|default("default"),
	theme: page.theme|default("default"),
	activePage: page.activePage|default(0),
	mainNav: page.mainNav|default(links.mainNav),
	secondaryNav: page.secondaryNav|default(links.secondaryNav)
} %}

{% block main %}
{% endblock %}

{% include '../partials/base/footer.twig' with {
	mainNav: page.mainNav|default(links.mainNav),
	secondaryNav: page.secondaryNav|default(links.secondaryNav),
	footerNav: page.footerNav|default(links.footerNav),
	socialNav: page.socialNav|default(links.socialNav)
} %}
